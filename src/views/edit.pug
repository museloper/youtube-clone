extends layout

block content
  h4 Change Title of video
  form(method='post')
    input(
      name='title',
      placeholder='input new title',
      value=video.title,
      required
    )
    input(value='Save', type='submit')
